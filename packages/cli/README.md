# IKEA planner CLI

This package is the command-line adapter over the METHOD and Storage One package interfaces. Run it from the repository root with:

```bash
node packages/cli/bin/ikea-assets.js --help
```
